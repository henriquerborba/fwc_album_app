import 'package:flutter/material.dart';

class MyStrickersPage extends StatelessWidget {

  const MyStrickersPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Minhas figurinhas'),),
           body: Container(),
       );
  }
}